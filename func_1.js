// Write a function greet(name) that takes a name as an  argument and 
// returns a greeting string, e.g., "Hello, [name]!"

function greet(name){
    return `Hii,${name}!!`
  }
  let ans= greet("Karthik")
  console.log(ans)